using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Net.Http.Headers;
using System.Web.Http;

using System.Web.Script.Serialization;
using Newtonsoft.Json;
using System.Web;

namespace TestWebAPI2.Controllers
{

    public class ProductsController : ApiController
    {
        //ProductFactory factory = new ProductFactory();
        //Product[] products;

        //[HttpGet]
        ////[ActionName("Thumbnail")]
        //public HttpResponseMessage FindProduct(int id)
        //{
        //    try
        //    {
        //        factory.moveTo(id);
        //        var product = factory.getCurrent();

        //        return JsonHelper.toJson(product);
        //    }
        //    catch (Exception)
        //    {
        //        var err = new { err = "recordNotFound" };
        //        return JsonHelper.toJson(err);
        //    }

        //}

        //[HttpGet]
        //public HttpResponseMessage GetAllProducts()
        //{
        //    products = factory.getAll();
        //    return JsonHelper.toJson(products);
        //}

        //[HttpPost]
        //public HttpResponseMessage addProduct(HttpRequestMessage requestMsg)
        //{
        //    var rrr = requestMsg.Content.ReadAsStringAsync().Result;
        //    //var obj = JsonHelper.fromJson(result);
        //    var dict = HttpUtility.ParseQueryString(rrr);



        //    var response = new HttpResponseMessage();
        //    response.Content = new StringContent(rrr);
        //    //response.Content.Headers.ContentType = new MediaTypeHeaderValue("text/html");
        //    return response;

        //}

        // POST: api/products

        //public Person Post([FromBody]Person person)
        //{
        //    if(person.name)
        //    return person;
        //}











    }
}
